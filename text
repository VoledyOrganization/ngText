 A：提交文件：git add .
*B：提交记录说明：git commit -m "提交记录说明"
*C：提交文件到github： git push -u origin master
  1：查看环境变量：git
  2：查看版本号：git version
  3：创建仓库：git   init
  4：克隆文件：git clone
*5：刷新文件：git   pull
  6：追踪文件：git   status
  7：查看分支：git branch
  8：查看远程分支：git branch -a
  9：创建分支并切换过去：git checkout -b sdk [sdk分支名]
  10：创建分支：git branch sdk [sdk分支名]
  11：切换分支：git checkout sdk [sdk分支名]
  12：删除分支：git branch-d sdk [sdk分支名]
  13：切换到主分支：git checkout master
*14：在主分支上合并代码：git merge sdk [sdk分支名]
  15：查看历史记录：git log 【git log会显示很多信息，如不想看详细信息，直接执行 git log --pretty=oneline】
  16：推动到远程分支：git push origin sdk [sdk远程分支名]
  17：查看本地跟踪分支对应的远程分支：git branch -vv
  18：查看远程仓库：git  remote  -v
  19：比较本地分支版本和远程版本的不同：git diff sdk(本地分支) origin/xxx(远程分支)

就是每次在本地分支上改动文件后，都要推动到远程服务器端的分支，然后再切换到主分支合并文件，最后再推动到远程服务器上

















